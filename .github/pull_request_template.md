## What this changes

<!-- What, and why. -->

## PR bar

- [ ] `zsh build.sh` is clean, no new warnings.
- [ ] Every suite in `tests/` is green.
- [ ] `./install.sh && ./maester-doctor` reports 0 failing.
- [ ] A new or changed provider ships fixtures for its failure modes, not
      only its happy path.
- [ ] A new `#available` guard adds a row to the feature ceiling table in
      `README.md`.
- [ ] No comments in code.
